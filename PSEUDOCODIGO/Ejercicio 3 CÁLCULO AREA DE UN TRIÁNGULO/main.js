function triangulo(){
	var a=parseFloat(prompt("ingresa la base: "));
	var b=parseFloat(prompt("ingresa la altura: "));
	var total = 0;

	total = (a*b)/2;
	alert("El área del triangulo es "+total );
}
triangulo();
